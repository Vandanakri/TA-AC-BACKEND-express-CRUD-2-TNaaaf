<%- include('partial/header') %>
  <h1>User list</h1>
  <!-- add each user -->
  <% users.forEach(user=> { %>
    <a href="/users/<%= user.id %>">
      <h3>
        <%= user.name %>
      </h3>
    </a>
    <% }) %>
      <%- include('partial/footer') %>